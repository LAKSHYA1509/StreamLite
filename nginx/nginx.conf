worker_processes  1;

events {
    worker_connections  1024;
}

rtmp {
    server {
        listen 1935;
        chunk_size 4096;

        # 'live' is the application name OBS will stream to
        application live {
            live on;
            record off;
        }
    }
}